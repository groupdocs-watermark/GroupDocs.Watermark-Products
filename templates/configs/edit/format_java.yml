inherits: ..\\languages-formats.yml

dimensions:
- alias: product
  values: [java]
- alias:  operation
  values: [edit]
- alias:  fileformat
  values: [doc, docx, pdf, ppt, pptx, rtf, xls, xlsx]

generationRules:
  include:
  - fileformat[doc, docx, pdf, ppt, pptx, rtf, xls, xlsx]
  
outputPath: "watermark\\{product}\\adjust\\{fileformat}.{lang}.md"

dictionaries: [ ..\\..\\data\\products.json ]
