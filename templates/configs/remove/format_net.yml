inherits: ..\\languages-formats.yml

dimensions:
- alias: product
  values: [net]
- alias:  operation
  values: [remove]
- alias:  fileformat
  values: [doc, docx, pdf, ppt, pptx, rtf, xls, xlsx]

generationRules:
  include:
  - fileformat[doc, docx, pdf, ppt, pptx, rtf, xls, xlsx]
  
outputPath: "watermark\\{product}\\remove\\{fileformat}.{lang}.md"

dictionaries: [ ..\\..\\data\\products.json ]
